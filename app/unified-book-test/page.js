'use client'

import UnifiedBookUploader from '../../components/UnifiedBookUploader'

export default function UnifiedBookTestPage() {
  return (
    <div className="min-h-screen bg-gray-50">
      <UnifiedBookUploader />
    </div>
  )
}